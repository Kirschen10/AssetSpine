
To clear all the contents of the table in SQL you will use command  - TRUNCATE TABLE tbl_directories;

To activate the interface with the Internet, write in the terminal - 
cd frontEnd
npm run dev

now click the URL to open the project in the browser

Activating the communication with the database, write in a new terminal - 
cd Backend   
npm start